# Agents Directory

This folder contains the agent specifications used by the project.
Each agent is defined using Markdown and includes its role, capabilities, input/output schemas, and behavior rules.

## Available Agents
- `planner.md` – Breaks down user requests and generates execution steps.
- `executor.md` – Executes the steps generated by the planner.
