# accounts 模块后续工作实施计划

> **计划日期**：2025-01-20  
> **计划目标**：完成 accounts 模块的后续优化工作  
> **计划方法**：Sequential Thinking + Software Planning Tool

---

## 📋 执行摘要

基于 Sequential Thinking 分析，后续工作按优先级排序：

1. **权限检查**（安全性，高优先级）
2. **完善团队视角功能**（功能完整性，中优先级）
3. **更新菜单链接**（配置更新，中优先级）
4. **优化错误处理**（代码质量，低优先级）

---

## 🎯 任务 1：添加权限检查（高优先级）

### 需求分析

**问题**：
- `form` 组件缺少权限检查
- 用户可能能够编辑不应该编辑的账户
- 存在安全风险

**目标**：
- 用户只能编辑自己的账户
- 组织拥有者只能编辑自己拥有的组织
- 无权限时显示错误并重定向

### 技术实现

**数据模型分析**：
- Account 表没有 `owner_id` 字段
- Organization 账户通过 `auth_organization_id` 记录创建者（auth.users.id）
- 需要检查当前用户是否是账户的创建者

**实现方案**：
1. 在 `form` 组件初始化时验证权限
2. 获取当前用户信息（通过 `WorkspaceContextFacade` 或 `AuthFacade`）
3. 检查账户的 `auth_user_id` 是否匹配当前用户
4. 对于组织账户，还需要检查用户是否是组织拥有者（通过 `organization_members` 表）

### 实施步骤

#### 步骤 1.1：在 form 组件中添加权限检查方法

```typescript
// src/app/routes/accounts/form/account-form.component.ts

private async checkEditPermission(accountId: string): Promise<boolean> {
  // 1. 获取当前用户信息
  const currentUserAuthId = this.tokenService.get()?.['user']?.id;
  if (!currentUserAuthId) {
    return false;
  }

  // 2. 加载账户信息
  const account = await this.accountService.loadAccountById(accountId);
  if (!account) {
    return false;
  }

  // 3. 检查是否是账户创建者
  if (account.auth_user_id === currentUserAuthId) {
    return true;
  }

  // 4. 对于组织账户，检查是否是组织拥有者
  if (account.type === AccountType.ORGANIZATION) {
    // 通过 organization_members 表检查用户是否是组织拥有者
    // 需要查询 organization_members 表，检查 role = 'owner'
    // 这里需要 OrganizationMemberService 提供方法
  }

  return false;
}
```

#### 步骤 1.2：在 ngOnInit 中调用权限检查

```typescript
async ngOnInit(): Promise<void> {
  const accountId = this.route.snapshot.paramMap.get('id');
  if (!accountId) {
    this.goBack();
    return;
  }

  // 验证权限
  const hasPermission = await this.checkEditPermission(accountId);
  if (!hasPermission) {
    this.message.error('您沒有權限編輯此帳戶');
    this.goBack();
    return;
  }

  // 继续加载账户信息
  this.loadAccount(accountId);
}
```

#### 步骤 1.3：添加 OrganizationMemberService 方法（如需要）

```typescript
// src/app/shared/services/organization-member/organization-member.service.ts

async isOrganizationOwner(organizationId: string, userId: string): Promise<boolean> {
  // 查询 organization_members 表
  // 检查用户是否是组织拥有者
}
```

### 验证检查清单

- [ ] 用户只能编辑自己的账户
- [ ] 组织拥有者只能编辑自己拥有的组织
- [ ] 无权限时显示错误消息
- [ ] 无权限时重定向到组织列表
- [ ] 权限检查不影响正常编辑流程

---

## 🎯 任务 2：完善团队视角功能（中优先级）

### 需求分析

**问题**：
- 团队视角中组织名称显示为占位符
- 需要加载团队信息以获取组织 ID
- 需要显示正确的组织名称

**目标**：
- 在团队视角中加载团队信息
- 从团队信息中获取 `organization_id`
- 显示正确的组织名称
- 实现切换到组织视角的功能

### 技术实现

**数据模型分析**：
- Team 模型有 `organization_id` 字段
- TeamService 有 `loadTeamById` 方法
- 可以通过 `WorkspaceContextFacade` 或 `AccountService` 获取组织信息

**实现方案**：
1. 在团队视角中注入 `TeamService`
2. 从 `contextId` 获取团队 ID
3. 加载团队信息
4. 从团队信息中获取 `organization_id`
5. 加载组织信息并显示

### 实施步骤

#### 步骤 2.1：在 org.component.ts 中添加团队信息加载

```typescript
// src/app/routes/accounts/org/org.component.ts

import { TeamService } from '@shared';

export class OrgComponent implements OnInit {
  private readonly teamService = inject(TeamService);
  
  // 添加团队信息状态
  readonly team = computed(() => this.teamService.selectedTeam());
  readonly loadingTeam = this.teamService.loading;
  
  // 更新 organizationName computed
  readonly organizationName = computed(() => {
    const type = this.contextType();
    const id = this.contextId();
    
    if (type === 'organization' && id) {
      const org = this.allOrganizations().find(o => o.id === id);
      return org?.name || '組織';
    }
    
    if (type === 'team' && id) {
      // 从团队信息中获取组织 ID
      const team = this.team();
      if (team && team.organization_id) {
        const org = this.allOrganizations().find(o => o.id === team.organization_id);
        return org?.name || '組織';
      }
      return '組織';
    }
    
    return '';
  });
  
  async ngOnInit(): Promise<void> {
    const type = this.contextType();
    const authUserId = this.tokenService.get()?.['user']?.id;
    
    if (type === 'user' && authUserId) {
      // 加载组织列表
      if (this.createdOrganizations().length === 0 && this.joinedOrganizations().length === 0 && !this.loadingOrganizations()) {
        await this.contextFacade.loadWorkspaceData(authUserId).catch(error => {
          console.error('載入組織列表失敗:', error);
        });
      }
    } else if (type === 'team' && id) {
      // 加载团队信息
      await this.teamService.loadTeamById(id).catch(error => {
        console.error('載入團隊信息失敗:', error);
        this.message.error('載入團隊信息失敗');
      });
    }
  }
  
  switchToOrganization(): void {
    const team = this.team();
    if (team && team.organization_id) {
      this.contextFacade.switchToOrganization(team.organization_id);
    } else {
      this.message.warning('無法獲取組織 ID');
    }
  }
}
```

### 验证检查清单

- [ ] 团队视角中正确显示组织名称
- [ ] 切换到组织视角功能正常工作
- [ ] 团队信息加载失败时显示错误
- [ ] 不影响其他视角的功能

---

## 🎯 任务 3：更新菜单链接（中优先级）

### 需求分析

**问题**：
- `user-data.json` 中的 schedules 链接指向 `/accounts/schedules`
- 实际路由已改为 `/accounts/org/:id/schedules`
- 需要更新菜单链接

**目标**：
- 更新菜单链接以匹配新的路由结构
- 考虑如何传递组织 ID（可能需要动态生成）

### 技术实现

**菜单系统分析**：
- 菜单数据文件位于 `src/assets/tmp/user-data.json`
- 菜单系统可能需要支持动态链接（带参数）
- 需要了解菜单系统如何处理动态参数

**实现方案**：
1. 更新 `user-data.json` 中的 schedules 链接
2. 如果菜单系统支持动态链接，使用 `:id` 占位符
3. 如果不支持，可能需要移除该菜单项或使用其他方式

### 实施步骤

#### 步骤 3.1：更新 user-data.json

```json
// src/assets/tmp/user-data.json

{
  "text": "My Schedules",
  "link": "/accounts/org/:id/schedules",
  "i18n": "menu.personal.account.schedules"
}
```

**注意**：如果菜单系统不支持动态链接，可能需要：
- 移除该菜单项
- 或者通过其他方式（如从组织列表页面导航）

### 验证检查清单

- [ ] 菜单链接更新正确
- [ ] 菜单系统正确处理动态链接（如支持）
- [ ] 如果不支持动态链接，有替代方案

---

## 🎯 任务 4：优化错误处理（低优先级）

### 需求分析

**问题**：
- `WorkspaceDataService` 只有一个 `error` 状态
- 无法分别显示 `created` 和 `joined` 的错误
- 错误消息缺少上下文信息

**目标**：
- 考虑添加分别的错误状态（`createdError`, `joinedError`）
- 或者优化错误消息，包含更多上下文
- 不影响现有功能

### 技术实现

**当前实现分析**：
- `WorkspaceDataService` 使用单个 `error` 状态
- `WorkspaceContextFacade` 暴露该错误状态
- 多个组件可能依赖当前的错误处理方式

**实现方案**：
1. **方案 A**：添加分别的错误状态
   - 在 `WorkspaceDataService` 中添加 `createdError` 和 `joinedError`
   - 更新 `WorkspaceContextFacade` 暴露这些状态
   - 更新使用该服务的组件

2. **方案 B**：优化错误消息（推荐）
   - 保持单个 `error` 状态
   - 在错误消息中包含更多上下文信息
   - 例如："載入我的組織失敗：網絡錯誤"

### 实施步骤

#### 步骤 4.1：优化错误消息（方案 B）

```typescript
// src/app/shared/services/workspace-context/workspace-data.service.ts

async loadWorkspaceData(authUserId: string): Promise<void> {
  this.loadingOrganizationsState.set(true);
  this.loadingTeamsState.set(true);
  this.errorState.set(null);

  try {
    // ... 加载逻辑 ...
    
    // 分别处理 created 和 joined 的错误
    const [createdOrgs, joinedOrgs] = await Promise.all([
      this.accountService.getUserCreatedOrganizations(authUserId).catch(error => {
        const errorMessage = error instanceof Error ? error.message : '載入我的組織失敗';
        console.error('載入我的組織失敗:', error);
        // 可以记录到错误日志，但不设置全局错误状态
        return [];
      }),
      this.accountService.getUserJoinedOrganizations(userAccount.id).catch(error => {
        const errorMessage = error instanceof Error ? error.message : '載入我加入的組織失敗';
        console.error('載入我加入的組織失敗:', error);
        return [];
      })
    ]);
    
    // 如果两个都失败，设置错误状态
    if (createdOrgs.length === 0 && joinedOrgs.length === 0) {
      this.errorState.set('載入組織列表失敗，請稍後再試');
    }
    
    // ... 其他逻辑 ...
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : '載入用戶工作區數據失敗';
    this.errorState.set(errorMessage);
    throw error;
  } finally {
    this.loadingOrganizationsState.set(false);
    this.loadingTeamsState.set(false);
  }
}
```

### 验证检查清单

- [ ] 错误消息包含更多上下文信息
- [ ] 不影响现有功能
- [ ] 错误处理逻辑清晰
- [ ] 不影响其他使用该服务的组件

---

## 📊 实施优先级和时间估算

| 任务 | 优先级 | 复杂度 | 预计时间 | 依赖关系 |
|------|--------|--------|----------|----------|
| 任务 1：权限检查 | 高 | 中 | 2-3 小时 | 无 |
| 任务 2：完善团队视角 | 中 | 低 | 1-2 小时 | 无 |
| 任务 3：更新菜单链接 | 中 | 低 | 0.5 小时 | 无 |
| 任务 4：优化错误处理 | 低 | 中 | 1-2 小时 | 无 |

**总预计时间**：4.5-7.5 小时

---

## ✅ 总体验证检查清单

### 代码质量

- [ ] **类型检查**：`yarn type-check` 通过
- [ ] **代码检查**：`yarn lint` 通过
- [ ] **样式检查**：`yarn lint:style` 通过
- [ ] **构建检查**：`yarn build` 通过
- [ ] **运行时检查**：功能在浏览器中正常工作

### 功能验证

- [ ] 权限检查正常工作
- [ ] 团队视角显示正确的组织名称
- [ ] 菜单链接更新正确
- [ ] 错误处理优化不影响现有功能

### 架构验证

- [ ] 符合架构分层原则
- [ ] 符合 SRP 原则
- [ ] 状态管理规范
- [ ] 错误处理完善

---

## 📚 相关文档

- [accounts 模块评估报告](./accounts模块评估报告-2025-01-20.md)
- [accounts 模块实施总结](./accounts模块实施总结-2025-01-20.md)
- [org 模块代码分析报告](./org模块代码分析报告-2025-01-20.md)
- [org 模块三视角设计方案](./org模块三视角设计方案-2025-01-20.md)

---

**最後更新**：2025-01-20  
**維護者**：開發團隊

