# 代碼庫索引 (FYI - Codebase)

> 舊版 Repomix 快照內容已移至 `docs/Archive/fyi-codebase.md`，保留完整歷史記錄。

目前此頁預留給新的程式碼快照或摘要；需要更新時請重新執行 Repomix：

```bash
npx -y repomix --config repomix.config.json --output docs/fyi-codebase.md --style markdown --verbose
```

完成後，請確認：

1. 最新輸出是否反映當前提交。
2. 檔案體積可接受（必要時裁切路徑範圍）。
3. 於本頁記錄生成日期與範圍，方便後續比對。


