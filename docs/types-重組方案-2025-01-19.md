# Types 目錄重組方案

> 📋 **目的**：按照業務模組重新組織 `src/app/core/infra/types/` 目錄結構

**重組日期**：2025-01-19  
**當前狀態**：所有 types 文件平鋪在根目錄  
**目標狀態**：按業務模組組織，結構清晰

---

## 📊 當前結構分析

### 當前文件列表

```
src/app/core/infra/types/
├── account.types.ts          → 🔐 帳戶與身份系統
├── collaboration.types.ts    → 🤝 組織協作系統
├── permission.types.ts       → 🔒 權限系統
├── blueprint.types.ts        → 🎯 藍圖/專案系統
├── task.types.ts             → 📋 任務執行系統
├── quality.types.ts          → ✅ 品質驗收系統
├── issue.types.ts            → ⚠️ 問題追蹤系統
├── communication.types.ts    → 💬 協作溝通系統
├── data.types.ts             → 📊 資料分析系統
├── bot.types.ts              → 🤖 機器人系統
├── system.types.ts           → ⚙️ 系統管理
├── realtime.types.ts         → ⚡ 基礎設施（實時功能）
└── database.types.ts         → 🗄️ 基礎設施（Supabase 生成）
```

### 文件內容分析

| 文件 | 內容類型 | 業務模組 | 備註 |
|------|---------|---------|------|
| `database.types.ts` | Supabase 生成的類型 | 基礎設施 | 包含所有 51 張表的 Row/Insert/Update 類型 |
| `realtime.types.ts` | 實時功能類型 | 基礎設施 | 訂閱、連接狀態、事件類型等 |
| `account.types.ts` | 枚舉類型 | 帳戶與身份系統 | AccountType, AccountStatus, TeamMemberRole 等 |
| `collaboration.types.ts` | 枚舉類型 | 組織協作系統 | CollaborationType, CollaborationStatus, InvitationStatus |
| `permission.types.ts` | 枚舉類型 + 類型定義 | 權限系統 | BranchPermissionLevel, PermissionAction, PermissionMatrix |
| `blueprint.types.ts` | 枚舉類型 | 藍圖/專案系統 | BlueprintStatus, BranchType, BranchStatus, PRStatus |
| `task.types.ts` | 枚舉類型 | 任務執行系統 | TaskType, TaskStatus, TaskPriority, TaskAssigneeType 等 |
| `quality.types.ts` | 枚舉類型 | 品質驗收系統 | QualityCheckStatus, QcPhotoType, InspectionPhotoType |
| `issue.types.ts` | 枚舉類型 | 問題追蹤系統 | IssueStatus, IssuePriority, IssueSeverity, IssueSyncStatus |
| `communication.types.ts` | 枚舉類型 | 協作溝通系統 | CommentableType, NotificationType, NotificationStatus 等 |
| `data.types.ts` | 枚舉類型 | 資料分析系統 | DocumentType, DocumentStatus, ReportPhotoType, DailyReportType |
| `bot.types.ts` | 枚舉類型 | 機器人系統 | BotStatus, BotTaskStatus, BotExecutionStatus |
| `system.types.ts` | 枚舉類型 | 系統管理 | FeatureFlagStatus, SettingCategory, ActivityLogAction 等 |

---

## 🎯 重組方案

### 目標結構

```
src/app/core/infra/types/
├── common/                          # 基礎設施類型（共用）
│   ├── database.types.ts           # Supabase 生成的類型（所有 51 張表）
│   ├── realtime.types.ts           # 實時功能類型
│   └── index.ts                    # 導出 common 類型
│
├── account/                         # 🔐 帳戶與身份系統
│   ├── account.types.ts
│   └── index.ts
│
├── org/                             # 🤝 組織協作系統
│   ├── collaboration.types.ts
│   └── index.ts
│
├── permission/                      # 🔒 權限系統
│   ├── permission.types.ts
│   └── index.ts
│
├── blueprint/                       # 🎯 藍圖/專案系統
│   ├── blueprint.types.ts
│   └── index.ts
│
├── task/                            # 📋 任務執行系統
│   ├── task.types.ts
│   └── index.ts
│
├── quality/                         # ✅ 品質驗收系統
│   ├── quality.types.ts
│   └── index.ts
│
├── issue/                           # ⚠️ 問題追蹤系統
│   ├── issue.types.ts
│   └── index.ts
│
├── collab/                          # 💬 協作溝通系統（評論、通知）
│   ├── communication.types.ts
│   └── index.ts
│
├── analytics/                       # 📊 資料分析系統（分析、文件）
│   ├── data.types.ts
│   └── index.ts
│
├── bot/                             # 🤖 機器人系統
│   ├── bot.types.ts
│   └── index.ts
│
├── system/                          # ⚙️ 系統管理
│   ├── system.types.ts
│   └── index.ts
│
└── index.ts                         # 統一導出所有類型
```

---

## 📋 詳細重組計劃

### 1. 基礎設施類型（common/）

**目的**：存放所有業務模組共用的基礎類型

#### common/database.types.ts
- **來源**：`database.types.ts`
- **內容**：Supabase 自動生成的所有 51 張表的類型定義
- **說明**：這是基礎類型，所有業務模組都會使用

#### common/realtime.types.ts
- **來源**：`realtime.types.ts`
- **內容**：實時功能相關類型（訂閱、連接狀態、事件類型等）
- **說明**：實時功能是基礎設施，多個業務模組都會使用

#### common/index.ts
```typescript
/**
 * 基礎設施類型統一導出
 */
export * from './database.types';
export * from './realtime.types';
```

---

### 2. 業務模組類型

#### account/account.types.ts
- **來源**：`account.types.ts`
- **內容**：
  - `AccountType` - 帳戶類型枚舉
  - `AccountStatus` - 帳戶狀態枚舉
  - `TeamMemberRole` - 團隊成員角色枚舉
  - `OrganizationMemberRole` - 組織成員角色枚舉

#### org/collaboration.types.ts
- **來源**：`collaboration.types.ts`
- **內容**：
  - `CollaborationType` - 協作類型枚舉
  - `CollaborationStatus` - 協作狀態枚舉
  - `InvitationStatus` - 邀請狀態枚舉

#### permission/permission.types.ts
- **來源**：`permission.types.ts`
- **內容**：
  - `BranchPermissionLevel` - 分支權限級別枚舉
  - `PermissionAction` - 權限操作類型枚舉
  - `PermissionMatrix` - 權限矩陣類型
  - `DEFAULT_PERMISSION_MATRIX` - 默認權限矩陣

#### blueprint/blueprint.types.ts
- **來源**：`blueprint.types.ts`
- **內容**：
  - `BlueprintStatus` - 藍圖狀態枚舉
  - `BranchType` - 分支類型枚舉
  - `BranchStatus` - 分支狀態枚舉
  - `PRStatus` - Pull Request 狀態枚舉

#### task/task.types.ts
- **來源**：`task.types.ts`
- **內容**：
  - `TaskType` - 任務類型枚舉
  - `TaskStatus` - 任務狀態枚舉
  - `TaskPriority` - 任務優先級枚舉
  - `TaskAssigneeType` - 任務指派類型枚舉
  - `TaskListType` - 任務列表類型枚舉
  - `TaskDependencyType` - 任務依賴類型枚舉

#### quality/quality.types.ts
- **來源**：`quality.types.ts`
- **內容**：
  - `QualityCheckStatus` - 品質檢查狀態枚舉
  - `QcPhotoType` - QC 照片類型枚舉
  - `InspectionPhotoType` - 檢查照片類型枚舉

#### issue/issue.types.ts
- **來源**：`issue.types.ts`
- **內容**：
  - `IssueStatus` - 問題狀態枚舉
  - `IssuePriority` - 問題優先級枚舉
  - `IssueSeverity` - 問題嚴重程度枚舉
  - `IssuePhotoType` - 問題照片類型枚舉
  - `IssueSyncStatus` - 問題同步狀態枚舉

#### collab/communication.types.ts
- **來源**：`communication.types.ts`
- **內容**：
  - `CommentableType` - 評論類型枚舉
  - `NotificationType` - 通知類型枚舉
  - `NotificationStatus` - 通知狀態枚舉
  - `NotificationPriority` - 通知優先級枚舉
  - `NotificationSubscriptionType` - 通知訂閱類型枚舉

#### analytics/data.types.ts
- **來源**：`data.types.ts`
- **內容**：
  - `DocumentType` - 文檔類型枚舉
  - `DocumentStatus` - 文檔狀態枚舉
  - `DocumentVersionStatus` - 文檔版本狀態枚舉
  - `ReportPhotoType` - 報告照片類型枚舉
  - `DailyReportType` - 日報類型枚舉

#### bot/bot.types.ts
- **來源**：`bot.types.ts`
- **內容**：
  - `BotStatus` - 機器人狀態枚舉
  - `BotTaskStatus` - 機器人任務狀態枚舉
  - `BotTaskPriority` - 機器人任務優先級枚舉
  - `BotExecutionStatus` - 機器人執行狀態枚舉

#### system/system.types.ts
- **來源**：`system.types.ts`
- **內容**：
  - `FeatureFlagStatus` - 功能標識狀態枚舉
  - `FeatureFlagTargetType` - 功能標識目標枚舉
  - `SettingCategory` - 設置分類枚舉
  - `SettingValueType` - 設置數據類型枚舉
  - `ActivityLogAction` - 活動日誌動作枚舉
  - `ActivityLogResourceType` - 活動日誌資源類型枚舉

---

### 3. 每個業務模組的 index.ts

每個業務模組都需要一個 `index.ts` 文件來導出該模組的所有類型：

```typescript
/**
 * 帳戶與身份系統類型定義
 * @module core/infra/types/account
 */
export * from './account.types';
```

---

### 4. 根目錄 index.ts（統一導出）

```typescript
/**
 * 類型定義模組統一導出
 * 
 * 按業務模組組織：
 * - common: 基礎設施類型（database, realtime）
 * - account: 帳戶與身份系統
 * - org: 組織協作系統
 * - permission: 權限系統
 * - blueprint: 藍圖/專案系統
 * - task: 任務執行系統
 * - quality: 品質驗收系統
 * - issue: 問題追蹤系統
 * - collab: 協作溝通系統
 * - analytics: 資料分析系統
 * - bot: 機器人系統
 * - system: 系統管理
 * 
 * @module core/infra/types
 */

// 基礎設施類型
export * from './common';

// 業務模組類型
export * from './account';
export * from './org';
export * from './permission';
export * from './blueprint';
export * from './task';
export * from './quality';
export * from './issue';
export * from './collab';
export * from './analytics';
export * from './bot';
export * from './system';
```

---

## 🔄 遷移步驟

### Step 1: 創建目錄結構

```bash
# 創建基礎設施目錄
mkdir -p src/app/core/infra/types/common

# 創建業務模組目錄
mkdir -p src/app/core/infra/types/account
mkdir -p src/app/core/infra/types/org
mkdir -p src/app/core/infra/types/permission
mkdir -p src/app/core/infra/types/blueprint
mkdir -p src/app/core/infra/types/task
mkdir -p src/app/core/infra/types/quality
mkdir -p src/app/core/infra/types/issue
mkdir -p src/app/core/infra/types/collab
mkdir -p src/app/core/infra/types/analytics
mkdir -p src/app/core/infra/types/bot
mkdir -p src/app/core/infra/types/system
```

### Step 2: 移動文件

```bash
# 移動基礎設施文件
mv src/app/core/infra/types/database.types.ts src/app/core/infra/types/common/
mv src/app/core/infra/types/realtime.types.ts src/app/core/infra/types/common/

# 移動業務模組文件
mv src/app/core/infra/types/account.types.ts src/app/core/infra/types/account/
mv src/app/core/infra/types/collaboration.types.ts src/app/core/infra/types/org/
mv src/app/core/infra/types/permission.types.ts src/app/core/infra/types/permission/
mv src/app/core/infra/types/blueprint.types.ts src/app/core/infra/types/blueprint/
mv src/app/core/infra/types/task.types.ts src/app/core/infra/types/task/
mv src/app/core/infra/types/quality.types.ts src/app/core/infra/types/quality/
mv src/app/core/infra/types/issue.types.ts src/app/core/infra/types/issue/
mv src/app/core/infra/types/communication.types.ts src/app/core/infra/types/collab/
mv src/app/core/infra/types/data.types.ts src/app/core/infra/types/analytics/
mv src/app/core/infra/types/bot.types.ts src/app/core/infra/types/bot/
mv src/app/core/infra/types/system.types.ts src/app/core/infra/types/system/
```

### Step 3: 更新導入路徑

需要更新所有引用這些類型的文件：

1. **Repositories** - `src/app/core/infra/repositories/**/*.ts`
2. **Facades** - `src/app/core/facades/**/*.ts`
3. **Services** - `src/app/shared/services/**/*.ts`
4. **Models** - `src/app/shared/models/**/*.ts`

### Step 4: 創建 index.ts 文件

為每個目錄創建 `index.ts` 文件，統一導出該模組的類型。

---

## 📝 注意事項

### 1. 導入路徑變更

**變更前**：
```typescript
import { AccountType, AccountStatus } from '@core/infra/types/account.types';
```

**變更後**：
```typescript
import { AccountType, AccountStatus } from '@core/infra/types/account';
// 或
import { AccountType, AccountStatus } from '@core';
```

### 2. Database 類型導入

**變更前**：
```typescript
import { Database } from '@core/infra/types/database.types';
```

**變更後**：
```typescript
import { Database } from '@core/infra/types/common';
// 或
import { Database } from '@core';
```

### 3. 向後兼容性

為了保持向後兼容，可以在根目錄的 `index.ts` 中重新導出：

```typescript
// 向後兼容：允許從舊路徑導入
export * from './common/database.types';
export * from './account/account.types';
// ... 其他文件
```

但建議逐步遷移到新的導入路徑。

---

## ✅ 重組後的優勢

1. **結構清晰**：按業務模組組織，易於查找和維護
2. **職責明確**：基礎設施類型與業務類型分離
3. **易於擴展**：新增業務模組時，只需在對應目錄添加文件
4. **符合架構**：與 services、models 的組織方式保持一致
5. **降低耦合**：各業務模組類型獨立，減少相互依賴

---

## 🎯 總結

重組後的目錄結構：
- **common/** - 基礎設施類型（database, realtime）
- **account/** - 帳戶與身份系統
- **org/** - 組織協作系統
- **permission/** - 權限系統
- **blueprint/** - 藍圖/專案系統
- **task/** - 任務執行系統
- **quality/** - 品質驗收系統
- **issue/** - 問題追蹤系統
- **collab/** - 協作溝通系統
- **analytics/** - 資料分析系統
- **bot/** - 機器人系統
- **system/** - 系統管理

**總計**：1 個基礎設施目錄 + 11 個業務模組目錄 = 12 個目錄

---

**方案制定日期**：2025-01-19  
**維護者**：開發團隊

