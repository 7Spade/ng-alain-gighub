# ng-alain 專案結構流程圖

> 📋 **目的**：以流程圖形式呈現專案核心層級，方便跨團隊快速理解職責分層與依賴

**最後更新**：2025-11-15  
**維護者**：開發團隊

---

```mermaid
flowchart TD
    %% =============== 頂層專案 =================
    Project["⚙️ ng-alain 專案"]

    %% =============== 前端應用層 =================
    subgraph Frontend["🅰️ 前端應用層 (src/)"]
        direction TB
        AppShell["App Shell\nmain.ts / app.config.ts"]

        subgraph AppLayer["📦 app/" ]
            direction TB
            Core["🧠 core/"]
            subgraph CoreModules["核心服務 / Infrastructure" ]
                direction LR
                CoreAccount["account"]
                CoreBlueprint["blueprint"]
                CoreI18n["i18n"]
                CoreNet["net"]
                CoreStartup["startup"]
                CoreInfra["infra"]
                CorePermissions["permissions"]
                CoreSupabase["supabase"]
            end
            Layout["🏗️ layout/"]
            Routes["🛣️ routes/"]
            subgraph FeatureRoutes["功能路由群"]
                direction LR
                RouteAccount["account"]
                RouteBlueprint["blueprint"]
                RouteDashboard["dashboard"]
                RouteDelon["delon"]
                RouteOrg["org"]
                RoutePro["pro"]
                RouteExtras["extras"]
                RouteWidgets["widgets"]
            end
            Shared["🧰 shared/"]
            subgraph SharedModules["共享資源 / Services"]
                direction LR
                SharedComponents["components"]
                SharedWidgets["st-widget"]
                SharedJsonSchema["json-schema"]
                SharedModels["models"]
                SharedServices["services"]
                SharedUtils["utils"]
                SharedImports["shared-imports"]
            end
        end

        Assets["🖼️ assets/ (tmp/, i18n/)"]
        Environments["🌐 environments/"]
        Styles["🎨 styles/, styles.less"]
        Testing["🧪 testing/"]
    end

    %% =============== Supabase 後端層 =================
    subgraph Supabase["🗃️ Supabase 後端層"]
        direction TB
        Functions["⚡ functions/ai"]
        Migrations["📜 migrations/"]
        Seed["🌱 seed.sql"]
        Roles["🛡️ roles.sql"]
        Config["⚙️ config.toml"]
    end

    %% =============== 文件與知識層 =================
    subgraph Docs["📚 文件與知識層 (docs/)"]
        direction TB
        Architecture["🏛️ 架構圖 01-~17-"]
        Guides["📝 作業指引 00-"]
        DataCatalogue["📊 資料表清單 18-"]
        Checklists["✅ 開發前檢查清單 19-"]
    end

    %% =============== 支援與工具層 =================
    subgraph Tooling["🛠️ 支援與工具層"]
        direction TB
        Mock["🎯 _mock/"]
        Scripts["🔁 scripts/_ci"]
        Public["🌐 public/"]
        E2E["🧭 e2e/"]
        CustomModes["🤖 custom_modes/"]
        GraphBank["🧭 graph-bank/"]
    end

    %% =============== 關聯 =================
    Project --> Frontend
    Project --> Supabase
    Project --> Docs
    Project --> Tooling

    AppShell --> AppLayer
    AppLayer --> Core
    AppLayer --> Layout
    AppLayer --> Routes
    AppLayer --> Shared
    Routes --> FeatureRoutes
    Shared --> SharedModules
    Core --> CoreModules
    Core -->|AGENTS.md| Docs

    Project -.lint/type-check.-> Tooling
    Tooling -.CI 管道.-> Supabase
    Docs -.設計對齊.-> Frontend

    %% =============== 樣式定義 =================
    classDef project fill:#263238,stroke:#000,color:#fff,stroke-width:2px
    classDef frontend fill:#4CAF50,stroke:#1B5E20,color:#fff
    classDef supabase fill:#FF9800,stroke:#E65100,color:#fff
    classDef docs fill:#2196F3,stroke:#0D47A1,color:#fff
    classDef tooling fill:#9C27B0,stroke:#6A1B9A,color:#fff
    classDef module fill:#CFD8DC,stroke:#546E7A,color:#000

    class Project project
    class Frontend AppLayer Assets Environments Styles Testing frontend
    class Supabase Functions Migrations Seed Roles Config supabase
    class Docs Architecture Guides DataCatalogue Checklists docs
    class Tooling Mock Scripts Public E2E CustomModes GraphBank tooling
    class CoreModules FeatureRoutes SharedModules module
```

## 使用說明

- 以「層」為分段：Frontend、Supabase、Docs、Tooling，對應專案實際責任分界。
- 子節點對應主要目錄或模組群組，保持與 `docs/02-專案結構樹.md` 一致。
- 虛線表示跨層依賴，例如文件對齊設計、CI 管道串接。
- 若調整專案結構，請同步更新此文件與實際目錄，以確保設計文件與實作一致。
