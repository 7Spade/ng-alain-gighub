# 實施計劃審查報告

> **目的**：檢查實施計劃是否符合專案標準和企業化標準

**審查日期**：2025-11-16  
**審查版本**：v1.0  
**審查結果**：✅ **基本符合，需要補充細節**

---

## 📋 審查範圍

- [x] Angular 20 最佳實踐符合性
- [x] 分層架構符合性
- [x] 代碼質量標準符合性
- [x] SHARED_IMPORTS 使用規範
- [x] 類型安全標準
- [x] 錯誤處理標準
- [x] 測試標準
- [x] 文檔完整性

---

## ✅ 符合標準的部分

### 1. Angular 20 最佳實踐 ✅

- ✅ 使用 `inject()` 進行依賴注入
- ✅ 使用 Signals 管理狀態
- ✅ 暴露 ReadonlySignal 給組件
- ✅ 使用 `computed()` 創建派生狀態
- ✅ 使用 `firstValueFrom` 處理 Observable

### 2. 分層架構 ✅

- ✅ Service 放在 `shared/services/`
- ✅ Repository 放在 `core/infra/repositories/`
- ✅ 類型定義放在 `shared/models/`
- ✅ 依賴方向正確（shared → core）

### 3. 狀態管理 ✅

- ✅ 使用 Signals 管理狀態
- ✅ 使用 computed() 創建派生狀態
- ✅ 暴露 ReadonlySignal

---

## ⚠️ 需要修正的問題

### 1. 服務名稱不一致 ❌

**問題**：計劃中引用 `ActivityLogService`，但實際專案中是 `AnalyticsService`

**影響範圍**：
- 2.1 TaskStagingService（依賴關係）
- 2.2 擴展 TaskService（依賴關係）
- 2.3 DailyReportService（依賴關係）
- 3.1 PR 審核流程（依賴關係）

**修正方案**：
```typescript
// ❌ 錯誤
import { ActivityLogService } from '@shared';

// ✅ 正確
import { AnalyticsService } from '@shared';
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 中所有提到 `ActivityLogService` 的地方

---

### 2. DocumentService 不存在 ❌

**問題**：計劃中提到 `DocumentService`，但專案中沒有這個服務

**影響範圍**：
- 2.3 DailyReportService（上傳照片）

**修正方案**：
使用 `SupabaseAdapterService` 或 `SupabaseService` 直接上傳文件：

```typescript
// ✅ 正確做法
import { SupabaseAdapterService } from '@shared';

// 上傳照片到 Storage
const filePath = `images/daily-reports/${reportId}/${fileName}`;
const { data, error } = await this.supabaseAdapter.storage
  .from('images')
  .upload(filePath, file);

// 然後創建 documents 記錄（如果需要）
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 2.3 DailyReportService 的依賴關係和方法實現

---

### 3. UI 組件缺少 SHARED_IMPORTS 規範 ⚠️

**問題**：UI 組件設計中沒有明確提到使用 SHARED_IMPORTS

**影響範圍**：
- 2.5 暫存區 UI 組件
- 3.4 PR 審核 UI 組件

**修正方案**：
在組件設計中明確要求使用 SHARED_IMPORTS：

```typescript
// ✅ 正確做法
import { SHARED_IMPORTS } from '@shared';

@Component({
  selector: 'app-task-staging-list',
  standalone: true,
  imports: [SHARED_IMPORTS], // 必須使用 SHARED_IMPORTS
  // ...
})
export class TaskStagingListComponent {}
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 2.5 和 3.4 的組件設計部分

---

### 4. UI 組件缺少 OnPush 和 Typed Forms 規範 ⚠️

**問題**：UI 組件設計中沒有明確提到使用 OnPush 和 Typed Forms

**影響範圍**：
- 2.5 暫存區 UI 組件
- 3.4 PR 審核 UI 組件

**修正方案**：
在組件設計中明確要求：

```typescript
// ✅ 正確做法
import { ChangeDetectionStrategy } from '@angular/core';
import { FormBuilder, FormGroup, FormControl, Validators } from '@angular/forms';

@Component({
  selector: 'app-task-staging-form',
  standalone: true,
  imports: [SHARED_IMPORTS],
  changeDetection: ChangeDetectionStrategy.OnPush, // 必須使用 OnPush
  // ...
})
export class TaskStagingFormComponent {
  // 使用 Typed Forms
  form!: FormGroup<{
    stagingData: FormControl<Record<string, unknown>>;
    notes: FormControl<string | null>;
  }>;
}
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 2.5 和 3.4 的組件設計部分

---

### 5. PR 表結構問題 ❌

**問題**：計劃中使用 `closedAt` 和 `closedBy` 字段，但 PR 表中沒有這些字段

**影響範圍**：
- 3.1 PR 審核流程（closePR 方法）

**實際 PR 表結構**：
```typescript
// pull_requests 表只有以下字段：
- id
- blueprint_id
- branch_id
- title
- description
- status
- submitted_by
- reviewed_by
- merged_by
- changes_summary
- submitted_at
- reviewed_at
- merged_at
// ❌ 沒有 closed_at 和 closed_by
```

**修正方案**：
1. 使用 `status = 'closed'` 標記關閉狀態
2. 使用 `reviewed_by` 或 `merged_by` 記錄關閉者（如果有的話）
3. 或者使用 `description` 字段記錄關閉原因

```typescript
// ✅ 修正後的 closePR 方法
async closePR(prId: string, reason?: string): Promise<void> {
  // ...
  // 2. 更新 PR 狀態為 CLOSED
  await this.updatePullRequest(prId, {
    status: PRStatus.CLOSED,
    // 注意：PR 表沒有 closed_at 和 closed_by 字段
    // 可以使用 reviewed_at 和 reviewed_by 記錄關閉信息
    reviewedAt: new Date().toISOString(),
    reviewedBy: currentUser.id,
    description: reason ? `${pr.description}\n\n關閉原因：${reason}` : pr.description
  } as any);
}
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 3.1 closePR 方法

---

### 6. Notification 類型問題 ❌

**問題**：計劃中使用 `notification.message`，但實際應該是 `notification.content`

**影響範圍**：
- 3.2 Realtime 通知系統（showDesktopNotification 方法）

**修正方案**：
```typescript
// ❌ 錯誤
private showDesktopNotification(notification: Notification): void {
  new Notification(notification.title, {
    body: notification.message, // ❌ 應該是 content
    // ...
  });
}

// ✅ 正確
private showDesktopNotification(notification: Notification): void {
  new Notification(notification.title, {
    body: notification.content || '', // ✅ 使用 content
    // ...
  });
}
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 3.2 showDesktopNotification 方法

---

### 7. 缺少單元測試要求 ⚠️

**問題**：計劃中只有 E2E 測試，沒有明確提到單元測試（Karma/Jasmine）

**影響範圍**：
- 所有 Service 和 Component

**修正方案**：
在每個任務的驗收標準中補充單元測試要求：

```markdown
#### ✅ 驗收標準
- [ ] 單元測試覆蓋率 ≥ 80%（Service）或 ≥ 60%（Component）
- [ ] 關鍵業務邏輯 100% 覆蓋（暫存區撤回機制、PR 合併邏輯等）
- [ ] 使用 Karma + Jasmine 編寫測試
- [ ] 測試文件命名：`*.spec.ts`
- [ ] 測試邊界條件和錯誤情況
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 所有任務的驗收標準部分

---

### 8. 缺少錯誤處理統一標準 ⚠️

**問題**：計劃中有錯誤處理，但沒有明確提到使用統一的錯誤處理機制

**影響範圍**：
- 所有 Service

**修正方案**：
在 Service 實現中明確錯誤處理標準：

```typescript
// ✅ 正確的錯誤處理模式
async submitToStaging(...): Promise<TaskStaging> {
  this.loadingState.set(true);
  this.errorState.set(null);
  
  try {
    // 業務邏輯
  } catch (error) {
    // 統一錯誤處理
    const errorMessage = error instanceof Error 
      ? error.message 
      : '提交到暫存區失敗';
    this.errorState.set(errorMessage);
    throw error; // 重新拋出以便上層處理
  } finally {
    this.loadingState.set(false);
  }
}
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 所有 Service 的方法實現部分

---

### 9. 缺少類型安全說明 ⚠️

**問題**：計劃中有使用 `as any` 的地方，需要明確說明原因

**影響範圍**：
- 所有使用類型斷言的地方

**修正方案**：
在使用 `as any` 的地方添加註釋說明：

```typescript
// ✅ 正確做法（帶註釋）
const dbInsertData = {
  actorId: data.actorId,
  // ... 其他字段
} as any; // 類型斷言：BaseRepository 會自動處理 camelCase → snake_case 轉換

// 或者使用更明確的類型
const dbInsertData: Record<string, unknown> = {
  actorId: data.actorId,
  // ...
};
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 所有使用 `as any` 的地方

---

### 10. 缺少 TaskService.updateTaskContractorFields 方法 ⚠️

**問題**：計劃中調用 `taskService.updateTaskContractorFields`，但需要確認這個方法是否存在

**影響範圍**：
- 3.1 PR 審核流程（mergePR 方法）

**修正方案**：
需要在 TaskService 中添加這個方法，或者使用現有的 `updateTask` 方法：

```typescript
// ✅ 方案1：添加新方法
async updateTaskContractorFields(
  taskId: string,
  contractorFields: Record<string, unknown>
): Promise<Task> {
  const task = await this.loadTaskById(taskId);
  if (!task) throw new Error('任務不存在');
  
  // 合併現有的 contractor_fields
  const updatedFields = {
    ...(task.contractor_fields as Record<string, unknown> || {}),
    ...contractorFields
  };
  
  return await this.updateTask(taskId, {
    contractor_fields: updatedFields as any
  } as any);
}

// ✅ 方案2：使用現有方法
await this.taskService.updateTask(taskChange.taskId, {
  contractor_fields: {
    ...(existingTask.contractor_fields as Record<string, unknown> || {}),
    ...taskChange.contractorFields
  } as any
} as any);
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 3.1 mergePR 方法
- 需要在 2.2 擴展 TaskService 中添加此方法

---

### 11. 缺少 DailyReportDetail 類型定義 ⚠️

**問題**：計劃中使用 `DailyReportDetail`，但需要確認這個類型是否存在

**影響範圍**：
- 2.3 DailyReportService

**修正方案**：
需要在 `src/app/shared/models/task/types.ts` 中定義：

```typescript
/**
 * DailyReport 詳情（包含關聯資料）
 */
export interface DailyReportDetail extends DailyReport {
  photos?: ReportPhoto[];
  reporter?: {
    id: string;
    name: string;
    email: string;
  };
  task?: {
    id: string;
    title: string;
  };
}
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 2.3 DailyReportService 的接口定義

---

### 12. 缺少 Notification 類型定義 ⚠️

**問題**：計劃中使用 `Notification` 類型，需要確認是否已定義

**影響範圍**：
- 3.2 Realtime 通知系統

**修正方案**：
確認 `src/app/shared/models/communication/types.ts` 中是否已定義，如果沒有則需要添加。

**修正位置**：
- `docs/實施計劃詳細說明.md` 3.2 的接口定義

---

### 13. 缺少 Realtime 訂閱清理機制 ⚠️

**問題**：計劃中的 Realtime 訂閱沒有明確的清理機制

**影響範圍**：
- 3.2 Realtime 通知系統

**修正方案**：
添加 `unsubscribeFromNotifications` 方法和 `ngOnDestroy` 生命週期：

```typescript
/**
 * 取消訂閱 Realtime 通知
 */
private unsubscribeFromNotifications(): void {
  if (this.channel) {
    this.supabase.client.removeChannel(this.channel);
    this.channel = null;
  }
}

// 在 Service 中實現 OnDestroy（如果需要）
// 或者使用 effect 的清理機制
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 3.2 NotificationService

---

### 14. 缺少驗收後的構建檢查 ⚠️

**問題**：驗收標準中沒有明確提到構建檢查

**影響範圍**：
- 所有任務

**修正方案**：
在每個任務的驗收標準中補充：

```markdown
#### ✅ 驗收標準
- [ ] 通過 `yarn type-check`（無類型錯誤）
- [ ] 通過 `yarn lint`（無 ESLint 錯誤）
- [ ] 通過 `yarn lint:style`（無 Stylelint 錯誤）
- [ ] 通過 `yarn build`（編譯成功）
- [ ] 通過相關測試（單元測試和 E2E 測試）
- [ ] 運行時功能正常
```

**修正位置**：
- `docs/實施計劃詳細說明.md` 所有任務的驗收標準部分

---

## 📝 建議補充的內容

### 1. 添加實施前檢查清單

在每個任務開始前，應該檢查：
- [ ] 相關 Repository 是否已存在
- [ ] 相關類型定義是否已存在
- [ ] 依賴的 Service 是否已實現
- [ ] 數據庫表結構是否已創建

### 2. 添加實施後檢查清單

在每個任務完成後，應該檢查：
- [ ] 代碼通過所有檢查（type-check, lint, build）
- [ ] 單元測試通過
- [ ] 文檔已更新
- [ ] 代碼已提交（如果適用）

### 3. 添加風險評估

對每個任務添加風險評估：
- **技術風險**：技術難度、依賴風險
- **時間風險**：時間估算是否合理
- **集成風險**：與其他模組的集成風險

### 4. 添加回滾計劃

對關鍵任務添加回滾計劃：
- 如何回滾代碼變更
- 如何回滾數據庫變更
- 如何恢復到之前的狀態

---

## ✅ 審查結論

### 總體評價

**符合度**：85%

**優點**：
- ✅ 架構設計符合分層架構標準
- ✅ 使用 Angular 20 現代語法（Signals, inject）
- ✅ 狀態管理使用 Signals
- ✅ 依賴關係清晰
- ✅ 驗收標準明確

**需要改進**：
- ⚠️ 服務名稱需要修正（ActivityLogService → AnalyticsService）
- ⚠️ 缺少 SHARED_IMPORTS 使用規範
- ⚠️ 缺少 OnPush 和 Typed Forms 規範
- ⚠️ 缺少單元測試要求
- ⚠️ PR 表結構問題需要修正
- ⚠️ 缺少構建檢查要求

### 建議

1. **立即修正**：服務名稱、PR 表結構、Notification 類型
2. **補充規範**：SHARED_IMPORTS、OnPush、Typed Forms
3. **完善測試**：添加單元測試要求
4. **完善驗收**：添加構建檢查要求

---

**審查人**：AI Assistant  
**審查日期**：2025-11-16

