# Component Documentation Template

- Component name:
- Module:
- File path(s):
- Selector (if Angular component):
- Type (page/modal/component/utility):
- Responsibilities (一句話):
- Inputs (@Input):
  - name: type — 描述
- Outputs (@Output):
  - name: EventEmitter<type> — 描述
- Exposed public methods:
  - methodName(params): returnType — 描述
- Uses services/facades/repositories:
  - ServiceName.methodX()
- Backend endpoints / RPC / Edge functions called:
  - POST /rpc/do_something — 呼叫時機、參數、成功結果
- State model (Signals / Facade):
  - read only signals consumed:
  - signals emitted:
- Side effects:
  - emits activity_log entries / triggers notification / triggers aggregation refresh
- Tests (required):
  - unit test cases (list)
  - e2e scenarios (list)
- Storybook story:
  - story name / knobs / variants
- Acceptance criteria (驗收條件):
  - e.g., 當 A 發生時應呼叫 ServiceX，並在 200ms 內更新 UI；錯誤時顯示錯誤 toast 並記錄 ErrorState