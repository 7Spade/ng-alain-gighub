# 任务模块推进策略

> **创建日期**：2025-01-15  
> **分析方法**：Sequential Thinking + Software Planning Tool + Context7 + Supabase MCP  
> **目标**：推进任务模块（模組4）从骨架状态到MVP功能完成

---

## 📊 当前状态分析

### ✅ 已完成
- ✅ 路由配置已完成（11个路由）
- ✅ 组件骨架已创建（显示"功能开发中"）
- ✅ 数据库表结构已创建（9张表，RLS已启用）
  - tasks（包含tree_path ltree类型和tree_level字段）
  - task_assignments
  - task_lists
  - task_staging
  - task_dependencies
  - task_templates
  - daily_reports
  - report_photos
  - weather_cache

### ❌ 待实现
- ❌ 数据模型层：未实现
- ❌ Repository层：未实现
- ❌ 服务层：未实现
- ❌ RLS策略：未实现（表已启用RLS，但策略未定义）

---

## 🎯 推进策略（分4个阶段）

### 阶段1：MVP基础（2-3周）

**目标**：实现基础CRUD功能，让任务系统可运行

#### 1.1 数据模型层（复杂度：6/10）

**位置**：`src/app/shared/models/task/`

**任务**：
- 创建 `types.ts` 文件，定义9张表的TypeScript类型
- 从 `Database` 类型中提取类型定义
- 创建枚举类型（TaskType、TaskStatus、TaskPriority等）
- 创建辅助类型（树状结构相关）

**参考**：`src/app/shared/models/account/types.ts`

**代码示例**：
```typescript
// shared/models/task/types.ts
import { Database } from '@core';

export type Task = Database['public']['Tables']['tasks']['Row'];
export type TaskInsert = Database['public']['Tables']['tasks']['Insert'];
export type TaskUpdate = Database['public']['Tables']['tasks']['Update'];

export enum TaskType {
  MILESTONE = 'milestone',
  PHASE = 'phase',
  TASK = 'task',
  SUBTASK = 'subtask'
}

export enum TaskStatus {
  PENDING = 'pending',
  ASSIGNED = 'assigned',
  IN_PROGRESS = 'in_progress',
  STAGING = 'staging',
  IN_QA = 'in_qa',
  IN_INSPECTION = 'in_inspection',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled'
}

export enum TaskPriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  URGENT = 'urgent'
}
```

#### 1.2 Repository层（复杂度：7/10）

**位置**：`src/app/core/infra/repositories/`

**任务**：
- 创建9个Repository类，继承 `BaseRepository`
- 重点：`TaskRepository` 需要实现树状结构查询方法
- 参考 `BlueprintRepository` 的实现模式

**关键方法**：
- `TaskRepository.findByBlueprintId()` - 按蓝图查询
- `TaskRepository.findChildren()` - 查询子任务
- `TaskRepository.findSubtree()` - 使用ltree查询子树（需要PostgreSQL RPC或原生SQL）

**参考**：`src/app/core/infra/repositories/blueprint.repository.ts`

#### 1.3 服务层（复杂度：6/10）

**位置**：`src/app/shared/services/task/`

**任务**：
- 创建 `TaskService`，实现基础CRUD操作
- 使用 Signals 管理状态
- 实现 `createTask()` - 创建任务，自动生成tree_path
- 实现 `updateTask()` - 更新任务
- 实现 `deleteTask()` - 删除任务，处理子任务
- 实现 `getTaskById()` - 获取任务详情
- 实现 `getTasksByBlueprint()` - 按蓝图查询

**参考**：`src/app/shared/services/account/account.service.ts`

#### 1.4 路由层（复杂度：5/10）

**位置**：`src/app/routes/tasks/`

**任务**：
- 实现 `TaskListComponent` - 任务列表，支持筛选（蓝图、状态、类型）
- 实现 `TaskDetailComponent` - 任务详情，显示完整信息和子任务
- 实现 `TaskFormComponent` - 创建/编辑任务表单

**技术要点**：
- 使用 `SHARED_IMPORTS`
- 使用 `nz-table` 组件（ng-zorro-antd）
- 使用 Signals 管理状态
- 连接 `TaskService`

**参考**：
- `src/app/routes/accounts/list/account-list.component.ts`
- `src/app/routes/blueprints/detail/blueprint-detail.component.ts`

---

### 阶段2：核心功能（2-3周）

**目标**：实现树状结构、任务指派、看板等核心业务功能

#### 2.1 树状结构管理（复杂度：8/10）

**任务**：
- 扩展 `TaskService`，实现树状结构管理
- `buildTaskTree()` - 构建树状结构
- `moveTask()` - 移动任务，更新tree_path
- `getTaskPath()` - 获取任务路径
- `validateTreeStructure()` - 验证树结构完整性

**技术要点**：
- 使用 PostgreSQL ltree 操作符（<@、@>、~等）
- 需要创建 Supabase RPC 函数或使用原生SQL
- 确保 tree_path 和 tree_level 的一致性

#### 2.2 任务指派功能（复杂度：6/10）

**任务**：
- 创建 `TaskAssignmentService`
- `assignTask()` - 支持四种类型：个人/团队/组织/承揽
- `unassignTask()` - 取消指派
- `getAssignmentsByTask()` - 获取任务的所有指派
- `getTasksByAssignee()` - 获取指派给某对象的所有任务

**技术要点**：
- 处理多对多关系
- 同时更新 `task_assignments` 和 `task_lists` 表

#### 2.3 路由层扩展（复杂度：7/10）

**任务**：
- `TaskBoardComponent` - 看板视图（待处理/进行中/已完成等列）
- `TaskCalendarComponent` - 日历视图，显示任务时间线
- `TaskAssignmentsComponent` - 任务指派管理界面
- `TaskTodoComponent` - 待办列表，聚合task_lists数据

**技术要点**：
- 使用 `nz-calendar` 组件（ng-zorro-antd）
- 使用 `nz-card` + `nz-list` 实现看板
- 参考 ng-zorro-antd 文档中的 Tree、Calendar 组件用法

---

### 阶段3：增强功能（2-3周）

**目标**：实现48h暂存、施工日志等增强功能

#### 3.1 48h暂存机制（复杂度：7/10）

**任务**：
- 创建 `TaskStagingService`
- `submitToStaging()` - 提交到暂存区
- `withdrawFromStaging()` - 48h内撤回
- `checkExpiredStaging()` - 检查过期，自动进入品管
- `getStagingTasks()` - 获取暂存中的任务

**技术要点**：
- 实现定时检查逻辑（可以使用 Supabase Edge Function 或前端定时器）
- 使用数据库触发器或 Edge Function 自动处理过期

#### 3.2 施工日志功能（复杂度：6/10）

**任务**：
- 创建 `DailyReportService`
- `createDailyReport()` - 创建日报
- `syncToMainBranch()` - 自动同步到主分支
- `getReportsByTask()` - 获取任务的日报列表
- 集成天气API（WeatherCache）

**技术要点**：
- 与蓝图系统的分支API集成
- 实现自动同步逻辑

#### 3.3 路由层完善（复杂度：6/10）

**任务**：
- `TaskStagingComponent` - 暂存区管理，显示48h倒计时
- `DailyReportsComponent` - 施工日志列表和创建表单
- `TaskPhotosComponent` - 施工照片管理，集成Supabase Storage

**技术要点**：
- 处理文件上传、图片预览
- EXIF数据提取
- Supabase Storage 集成

---

### 阶段4：完善（1-2周）

**目标**：RLS策略、测试、文档

#### 4.1 RLS策略实施（复杂度：8/10）

**任务**：
- 为9张任务相关表实施RLS策略
- `tasks`：拥有者可创建/修改，分支只能修改contractor_fields
- `task_assignments`：指派者和管理员可管理
- `task_staging`：提交者可撤回（48h内）
- `daily_reports`：创建者可修改，自动同步主分支

**技术要点**：
- 参考蓝图系统的RLS实现
- 确保符合Git-like分支模型的权限要求
- 使用 Supabase MCP 工具创建和验证RLS策略

#### 4.2 测试与文档（复杂度：5/10）

**任务**：
- 为所有Repository编写单元测试（目标80%覆盖率）
- 为Service层编写集成测试
- 更新API文档（`docs/33-API-接口詳細文檔.md`）
- 创建用户操作指南
- 更新路线图状态

**参考**：`docs/38-測試指南.md`

---

## 🔧 技术要点

### 1. 树状结构实现

**PostgreSQL ltree 操作**：
- `<@` - 左操作数是右操作数的后代
- `@>` - 左操作数是右操作数的祖先
- `~` - 匹配 lquery 模式

**实现方式**：
```typescript
// 需要使用 Supabase RPC 函数或原生SQL
// 示例：查询子树
findSubtree(treePath: string): Observable<Task[]> {
  return this.supabase.rpc('get_task_subtree', { path: treePath })
    .pipe(
      map(response => toCamelCaseData(response.data) as Task[])
    );
}
```

### 2. 48h暂存机制

**实现方式**：
- 方案1：使用 Supabase Edge Function 定时检查
- 方案2：前端定时器 + 服务端API
- 方案3：数据库触发器自动处理

**推荐**：方案1（Edge Function），更可靠

### 3. 任务指派多对多关系

**数据流**：
1. 创建 `task_assignments` 记录
2. 同步更新 `task_lists` 表
3. 发送通知（可选）

### 4. 施工日志同步

**流程**：
1. 创建 `daily_reports` 记录
2. 调用 `BranchService.syncToMainBranch()`
3. 更新主分支的 `activity_logs`

---

## 📋 UI组件选择

根据 ng-zorro-antd 文档和 Context7 查询结果：

| 功能 | 组件 | 说明 |
|------|------|------|
| 任务列表 | `nz-table` | 支持分页、排序、筛选 |
| 任务树 | `nz-tree` | 支持展开/折叠、选择 |
| 任务日历 | `nz-calendar` | 支持自定义日期单元格 |
| 任务看板 | `nz-card` + `nz-list` | 或使用第三方拖拽库 |
| 任务表单 | `nz-form` + `nz-form-item` | 响应式表单 |

**所有组件都在 `SHARED_IMPORTS` 中，可直接使用**

---

## 🔐 RLS策略设计

### tasks 表
- **拥有者（蓝图owner）**：可以创建/修改所有字段
- **分支组织**：只能修改 `contractor_fields` 字段
- **任务创建者**：可以修改自己创建的任务（在48h内或未指派前）

### task_staging 表
- **提交者**：可以在48h内撤回
- **拥有者**：可以查看所有暂存记录

### task_assignments 表
- **指派者**：可以创建/删除指派
- **管理员**：可以管理所有指派

---

## 📅 时间估算

| 阶段 | 时间 | 累计 |
|------|------|------|
| 阶段1：MVP基础 | 2-3周 | 2-3周 |
| 阶段2：核心功能 | 2-3周 | 4-6周 |
| 阶段3：增强功能 | 2-3周 | 6-9周 |
| 阶段4：完善 | 1-2周 | 7-11周 |

**总计**：7-11周

---

## 🚀 下一步行动

1. ✅ **确认数据库状态** - 已完成（9张表已存在）
2. ✅ **确认技术栈** - 已完成（ng-zorro组件、BaseRepository模式）
3. ⏳ **开始阶段1.1** - 创建数据模型层
4. ⏳ **创建Repository层** - 参考BlueprintRepository
5. ⏳ **实现服务层** - 使用Signals管理状态
6. ⏳ **实现UI层** - 使用SHARED_IMPORTS

---

## 📚 参考文档

- [44-專案路線圖.md](./44-專案路線圖.md) - 项目路线图
- [30-0-完整SQL表結構定義.md](./30-0-完整SQL表結構定義.md) - 数据库表结构
- [27-完整架構流程圖.mermaid.md](./27-完整架構流程圖.mermaid.md) - 系统架构
- [開發中模組狀態檢查報告-2025-01-15.md](./開發中模組狀態檢查報告-2025-01-15.md) - 当前状态

---

**最后更新**：2025-01-15  
**维护者**：开发团队

