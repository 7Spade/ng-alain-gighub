# 開發者工具腳本集

## 📑 目錄

- [📋 可用腳本](#-可用腳本)
  - [1. `quick-check.sh` - 快速環境檢查 ⭐](#1-quick-checksh---快速環境檢查-)
- [🔧 新增腳本指南](#-新增腳本指南)
  - [腳本命名規範](#腳本命名規範)
  - [腳本模板](#腳本模板)
  - [添加新腳本步驟](#添加新腳本步驟)
- [📝 腳本開發最佳實踐](#-腳本開發最佳實踐)
  - [1. 錯誤處理](#1-錯誤處理)
  - [2. 輸出友好](#2-輸出友好)
  - [3. 參數處理](#3-參數處理)
  - [4. 目錄檢查](#4-目錄檢查)
- [🚀 常用 Yarn 腳本](#-常用-yarn-腳本)
- [🎯 開發工作流程建議](#-開發工作流程建議)
  - [每日開始開發前](#每日開始開發前)
  - [Commit 前](#commit-前)
  - [Push 前](#push-前)
- [🔗 相關文檔](#-相關文檔)
- [📞 取得協助](#-取得協助)

---


> 🛠️ **目的**：提供實用的開發者腳本，提升開發效率和體驗

**最後更新**：2025-01-20
**維護者**：開發團隊

- --

## 📋 可用腳本

### 1. `quick-check.sh` - 快速環境檢查 ⭐

快速檢查開發環境是否正確設定，適合在開始開發前或遇到問題時執行。

**使用方式**：

```bash
# 從專案根目錄執行
./scripts/dev-tools/quick-check.sh
```

**檢查項目**：
- ✅ Node.js 版本（需要 >= 20）
- ✅ Yarn 版本
- ✅ Git 狀態
- ✅ 專案依賴（node_modules）
- ✅ 環境設定檔案
- ✅ Git Hooks（Husky）
- ✅ VSCode 設定

**輸出範例**：

```bash
   ng-alain-gighub 開發環境快速檢查
═══════════════════════════════════════════════════════════

1️⃣ 檢查必備工具
─────────────────────────────────────────────────────────
ℹ️  檢查 Node.js... ✅ v20.19.5
ℹ️  檢查 Yarn... ✅ 4.9.2
ℹ️  檢查 Git... ✅ git version 2.43.0
   ✅ Node.js 版本符合要求 (>= 20)

2️⃣ 檢查專案依賴
─────────────────────────────────────────────────────────
✅ node_modules 存在
✅ yarn.lock 存在

...

═══════════════════════════════════════════════════════════
   檢查結果總結
═══════════════════════════════════════════════════════════

✅ 通過: 15
⚠️  警告: 2
❌ 失敗: 0

✅ 所有檢查通過！環境準備就緒 🎉

可以開始開發了：
  • yarn start - 啟動開發伺服器
  • yarn hmr - 啟動 HMR 模式
  • yarn test - 執行測試
```

- --

## 🔧 新增腳本指南

### 腳本命名規範

- 使用 kebab-case：`my-script.sh`
- 檔名應清楚描述功能
- 使用 `.sh` 副檔名

### 腳本模板

```bash
#!/bin/bash
# 腳本描述
# Script Description

set -e  # 遇到錯誤時立即退出

# 顏色定義（可選）
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}開始執行...${NC}"

# 你的腳本邏輯
# ...

echo -e "${GREEN}完成！${NC}"
```

### 添加新腳本步驟

1. 創建腳本檔案：
   ```bash
   touch scripts/dev-tools/your-script.sh
   ```

2. 添加執行權限：
   ```bash
   chmod +x scripts/dev-tools/your-script.sh
   ```

3. 更新本 README

4. 測試腳本：
   ```bash
   ./scripts/dev-tools/your-script.sh
   ```

- --

## 📝 腳本開發最佳實踐

### 1. 錯誤處理

```bash
# 使用 set -e 確保錯誤時退出
set -e

# 或者自定義錯誤處理
if ! command -v node &> /dev/null; then
    echo "錯誤：Node.js 未安裝"
    exit 1
fi
```

### 2. 輸出友好

```bash
# 使用顏色和圖標
echo -e "${GREEN}✅ 成功${NC}"
echo -e "${RED}❌ 失敗${NC}"
echo -e "${YELLOW}⚠️  警告${NC}"
echo -e "${BLUE}ℹ️  資訊${NC}"
```

### 3. 參數處理

```bash
# 檢查參數
if [ $# -eq 0 ]; then
    echo "使用方式: $0 <參數>"
    exit 1
fi

# 使用參數
ARG1=$1
```

### 4. 目錄檢查

```bash
# 確保在專案根目錄執行
if [ ! -f "package.json" ]; then
    echo "錯誤：請在專案根目錄執行此腳本"
    exit 1
fi
```

- --

## 🚀 常用 Yarn 腳本

除了這些開發者工具腳本，專案中也有許多實用的 Yarn 腳本：

```bash
# 開發
yarn start          # 啟動開發伺服器
yarn hmr            # 啟動 HMR 模式

# 建置
yarn build          # 建置專案
yarn analyze        # 建置並產生 source map
yarn analyze:view   # 查看 bundle 分析

# 程式碼品質
yarn lint           # 執行完整 lint 檢查
yarn lint:ts        # 只檢查 TypeScript
yarn lint:style     # 只檢查樣式

# 測試
yarn test           # 執行測試（watch 模式）
yarn test-coverage  # 執行測試並產生覆蓋率報告

# 工具
yarn ng             # 執行 Angular CLI 命令
yarn prepare        # 設定 Git hooks
```

- --

## 🎯 開發工作流程建議

### 每日開始開發前

```bash
# 1. 拉取最新代碼
git pull

# 2. 更新依賴（如果 package.json 有變更）
yarn install

# 3. 快速環境檢查
./scripts/dev-tools/quick-check.sh

# 4. 啟動開發伺服器
yarn start
```

### Commit 前

```bash
# 1. Lint 檢查
yarn lint

# 2. 執行測試（可選）
yarn test

# 3. Commit（pre-commit hook 會自動檢查）
git commit -m "feat: 新功能"
```

### Push 前

```bash
# 確保建置成功
yarn build

# Push
git push
```

- --

## 🔗 相關文檔

- [開發者快速檢查清單](../../docs/60-開發者快速檢查清單.md)
- [快速開始指南](../../docs/25-快速開始指南.md)
- [開發最佳實踐指南](../../docs/42-開發最佳實踐指南.md)

- --

## 📞 取得協助

遇到問題時：

1. 查閱 [常見問題 FAQ](../../docs/29-常見問題-FAQ.md)
2. 執行 `./scripts/dev-tools/quick-check.sh` 診斷環境問題
3. 聯繫團隊成員

- --

**最後更新**：2025-01-20
**維護者**：開發團隊
