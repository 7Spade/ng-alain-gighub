# Shared Task Pipes

此目錄提供任務模組可重複使用的自訂管道。實作時請留意：

- 僅處理資料格式化或簡單轉換，禁止加入副作用、HTTP 請求或全域狀態修改。
- 優先檢查 Angular、@delon、ng-zorro 既有管道是否符合需求，避免重複造輪。
- 每個管道需具備明確型別定義與對應 `transform` 回傳型別，嚴禁使用 `any`。
- 如需相依其他服務，請透過 `inject()` 取得並保持可測性；必要時撰寫單元測試確保格式化結果。
- 在此 README 或檔案內補充使用情境與範例，協助其他模組快速導入。

