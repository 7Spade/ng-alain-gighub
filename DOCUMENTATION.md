# 文檔貢獻指南

> **目的**: 本指南說明如何為 ng-alain-gighub 專案貢獻文檔，確保所有文檔符合企業級品質標準。

## 目標讀者 (Audience)

- 文檔貢獻者
- 技術寫作人員
- AI Agents
- 專案維護者

---

## 📋 文檔標準

所有 Markdown 文檔必須遵循 [Markdown 文檔標準化規範](./.github/agents/markdown-documentation-standards.md)。

### 核心要求

#### 1. 檔案命名
✅ **使用 kebab-case（小寫字母+連字符）**

```
✅ 正確: getting-started.md, api-standards.md, code-review-guide.md
❌ 錯誤: Getting-Started.md, API_Standards.md, codeReviewGuide.md
```

**例外**: `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `LICENSE.md`, `AGENTS.md` 等根目錄特殊文件

#### 2. 標題層級
✅ **每個文件有且僅有一個 H1**  
✅ **按順序使用標題層級（H1 → H2 → H3 → H4），不跳級**

```markdown
✅ 正確:
# 主標題 (H1)
## 第一節 (H2)
### 第一節子項 (H3)
## 第二節 (H2)

❌ 錯誤:
# 主標題 (H1)
### 子標題 (H3)  ← 跳過 H2
# 另一個主標題 (H1)  ← 多個 H1
```

#### 3. 必要章節
指南類文檔（`docs/guides/`, `docs/specs/`, `docs/reference/`）必須包含:

```markdown
# 文檔標題

> **目的**: 說明文檔用途和解決的問題

## 目標讀者 (Audience)

列出目標讀者群（例如：前端開發者、AI Agents 等）

## 內容主體

[文檔主要內容...]
```

#### 4. 程式碼區塊
✅ **所有程式碼區塊必須標註語言**

````markdown
✅ 正確:
```typescript
export class Example {}
```

```bash
npm install
```

❌ 錯誤:
```
export class Example {}
```
````

常用語言標記: `typescript`, `javascript`, `html`, `css`, `less`, `bash`, `json`, `yaml`, `markdown`, `mermaid`

#### 5. 列表格式
✅ **統一使用 `-` 作為無序列表標記**

```markdown
✅ 正確:
- 第一項
- 第二項
  - 子項（兩個空格縮排）

❌ 錯誤:
* 第一項
+ 第二項
```

---

## 🛠️ 開發工具

### 1. 安裝必要工具

```bash
# 安裝 glob（文檔驗證腳本需要）
yarn add -D glob

# （可選）安裝 markdownlint
yarn add -D markdownlint-cli
```

### 2. 驗證文檔品質

在提交前驗證你的文檔：

```bash
# 驗證所有文檔
yarn docs:validate-all

# 驗證特定文件
yarn docs:validate docs/guides/getting-started.md

# 驗證特定目錄
yarn docs:validate "docs/guides/**/*.md"
```

### 3. 自動格式化（可選）

```bash
# 安裝 prettier
yarn add -D prettier

# 格式化 Markdown 文件
yarn prettier --write "**/*.md"
```

---

## 📝 提交流程

### 步驟 1: 創建或修改文檔

1. 遵循[檔案命名規範](#1-檔案命名)
2. 包含[必要章節](#3-必要章節)
3. 使用正確的[標題層級](#2-標題層級)
4. 為[程式碼區塊添加語言標記](#4-程式碼區塊)

### 步驟 2: 本地驗證

```bash
# 驗證你的文檔
yarn docs:validate path/to/your-file.md

# 確保沒有錯誤
```

### 步驟 3: 提交變更

```bash
git add docs/your-new-file.md
git commit -m "docs: add your documentation description"
```

### 步驟 4: 提交 Pull Request

1. 確保 PR 描述清楚說明文檔變更
2. 標記為 `documentation` label
3. 等待 code review

---

## ✅ 檢查清單

在提交文檔前，請確認:

- [ ] 檔案名稱使用 kebab-case
- [ ] 有且僅有一個 H1 標題
- [ ] 標題層級連續不跳級
- [ ] 包含「目的」和「目標讀者」章節（指南類文檔）
- [ ] 所有程式碼區塊都標註了語言
- [ ] 列表使用 `-` 標記
- [ ] 運行 `yarn docs:validate` 通過
- [ ] 內容準確無誤
- [ ] 範例清晰易懂

---

## 📚 參考資源

### 專案文檔
- [Markdown 文檔標準化規範](./.github/agents/markdown-documentation-standards.md) - 完整標準
- [文檔品質分析報告](./docs/markdown-documentation-quality-analysis-report.md) - 當前狀態
- [文檔索引](./docs/README.md) - 所有文檔導航

### 外部資源
- [Markdown Guide](https://www.markdownguide.org/) - Markdown 語法指南
- [GitHub Flavored Markdown](https://github.github.com/gfm/) - GFM 規範
- [Google 技術寫作指南](https://developers.google.com/tech-writing) - 技術寫作最佳實踐

---

## ❓ 常見問題

### Q: 為什麼要使用 kebab-case？
A: kebab-case 是 URL 友善格式，符合 Web 標準，且在 Linux/Unix 系統中相容性最好。

### Q: 程式碼區塊一定要標註語言嗎？
A: 是的。語言標記啟用語法高亮，大幅提升可讀性和專業度。

### Q: 如果我的文檔不是指南類型，還需要 Purpose/Audience 嗎？
A: 規範主要針對 `docs/guides/`, `docs/specs/`, `docs/reference/` 目錄。其他類型文檔建議添加但不強制。

### Q: 驗證工具報錯怎麼辦？
A: 根據錯誤訊息修正。如果是誤報，可在專案 Issues 中回報。

### Q: 可以用簡體中文嗎？
A: 本專案使用繁體中文（台灣用語），技術術語保留英文。

---

## 💡 最佳實踐

### 1. 清晰的標題
標題應清楚描述內容，避免模糊用語：

✅ 好: "如何設置開發環境"  
❌ 差: "設置"

### 2. 具體的範例
提供實際可運行的程式碼範例：

✅ 好: 包含完整的 import 和 class 定義  
❌ 差: 僅有片段程式碼

### 3. 及時更新
技術文檔需要定期更新：

- 新功能發布時更新相關文檔
- 廢棄功能時標記 deprecated
- 底部標註最後更新日期

### 4. 連結有效性
確保內部和外部連結有效：

```bash
# 使用 markdown-link-check 驗證連結
yarn add -D markdown-link-check
npx markdown-link-check docs/**/*.md
```

---

## 🚀 進階主題

### 自動化檢查

建議在 git pre-commit hook 中加入文檔驗證：

```bash
# .husky/pre-commit
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# 只驗證變更的 Markdown 文件
git diff --cached --name-only --diff-filter=ACM | grep '\.md$' | xargs -r yarn docs:validate
```

### CI/CD 整合

在 GitHub Actions 中添加文檔驗證：

```yaml
# .github/workflows/docs.yml
name: Documentation Quality Check

on: [pull_request]

jobs:
  validate-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: yarn install
      - run: yarn docs:validate-all
```

---

**最後更新**: 2025-01-21  
**維護者**: 開發團隊  
**版本**: 1.0.0

如有疑問，請在專案 Issues 中提出，標記 `documentation` label。
