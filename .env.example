# .env.example

# ⚠️ 重要提醒：請勿在任何檔案中提交真實的 Supabase 金鑰
# 此專案使用 Angular 環境配置（src/environments/environment.ts）
# 請確保 environment.ts 不會被提交到版本控制系統

# Supabase Configuration
# 請在 src/environments/environment.ts 中配置以下資訊：
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# 範例配置：
# export const environment = {
#   production: false,
#   supabase: {
#     url: process.env['SUPABASE_URL'] || 'https://your-project-id.supabase.co',
#     anonKey: process.env['SUPABASE_ANON_KEY'] || 'your-anon-key-here'
#   }
# };

# 安全建議：
# 1. 永遠不要在程式碼中硬編碼真實金鑰
# 2. 使用環境變數或安全的配置管理服務
# 3. 在 CI/CD 流程中使用 secrets 管理
# 4. 定期輪換 API 金鑰
# 5. 啟用 Supabase Row Level Security (RLS) 政策
